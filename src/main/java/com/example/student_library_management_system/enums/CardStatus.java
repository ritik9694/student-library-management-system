package com.example.student_library_management_system.enums;

public enum CardStatus {
    // enum is set of predefined values ->>>>means that cards can only take below value
    ACTIVE,
    DEACTIVATED,
    BLOCKED,
    EXPIRED,

}
